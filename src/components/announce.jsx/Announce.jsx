import React from "react";

const Announce = ({title}) => {
  return (
    <div className="bg-[#293241] font-bold h-6 text-white flex items-center justify-center">
      {title}
    </div>
  );
};

export default Announce;
